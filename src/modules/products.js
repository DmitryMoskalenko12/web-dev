const products = () => {
/* const wrapper = document.querySelector('.products__wrapper');
const viewBlock = document.querySelector('.products__view-block'); */

/* wrapper.addEventListener('mouseover', (event) => {
   if (event.target.classList.contains('products__card')) {
     viewBlock.classList.add('products__view-block_active');
   }
})

wrapper.addEventListener('mouseout', (event) => {
  if (!event.target.classList.contains('products__card')) {
    viewBlock.classList.remove('products__view-block_active');
  }
}) */
}

export default products;